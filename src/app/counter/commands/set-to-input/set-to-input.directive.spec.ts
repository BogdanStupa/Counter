import { SetToInputDirective } from './set-to-input.directive';

describe('SetToInputDirective', () => {
  it('should create an instance', () => {
    const directive = new SetToInputDirective();
    expect(directive).toBeTruthy();
  });
});
