import { CountDownBtnDirective } from './count-down-btn.directive';

describe('CountDownBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new CountDownBtnDirective();
    expect(directive).toBeTruthy();
  });
});
