import { CountUpBtnDirective } from './count-up-btn.directive';

describe('CountUpBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new CountUpBtnDirective();
    expect(directive).toBeTruthy();
  });
});
