import { TickSpeedInputDirective } from './tick-speed-input.directive';

describe('TickSpeedInputDirective', () => {
  it('should create an instance', () => {
    const directive = new TickSpeedInputDirective();
    expect(directive).toBeTruthy();
  });
});
