import { SetToBtnDirective } from './set-to-btn.directive';

describe('SetToBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new SetToBtnDirective();
    expect(directive).toBeTruthy();
  });
});
