import { StartBtnDirective } from './start-btn.directive';

describe('StartBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new StartBtnDirective();
    expect(directive).toBeTruthy();
  });
});
