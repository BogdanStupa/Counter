import { CountDiffInputDirective } from './count-diff-input.directive';

describe('CountDiffInputDirective', () => {
  it('should create an instance', () => {
    const directive = new CountDiffInputDirective();
    expect(directive).toBeTruthy();
  });
});
