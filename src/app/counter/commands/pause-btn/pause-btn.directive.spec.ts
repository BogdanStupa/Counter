import { PauseBtnDirective } from './pause-btn.directive';

describe('PauseBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new PauseBtnDirective();
    expect(directive).toBeTruthy();
  });
});
